import { StyleSheet } from "react-native";
import { paddingSize } from "../../styles/sizes";

export default StyleSheet.create({
    radioSpace: {
        paddingRight: paddingSize.medium
    },
    radioContainer: {
        alignItems: "center",
        paddingTop: paddingSize.large,
        paddingBottom: paddingSize.large
    }
});
